# TODO(provenance): exact-pin remaining ranges from a live pod's pip freeze at
# next rebuild (replicability audit G4). The entries below that are still
# `>=`/bare (spacy/openai/seqeval/scikit-learn/awesome-align/polars/fastapi/
# uvicorn/etc.) float to whatever resolved into the current image; capture
# their frozen versions before the next image rebuild so the image is
# reproducible from this file alone.

# === Core ML & Training ===
transformers==4.41.2
datasets==2.19.2
sentencepiece==0.2.0
protobuf==4.25.3
wandb==0.17.0

# === Linguistic Preprocessing (Ablations) ===
# spaCy is used for POS, dependency parsing, etc.
spacy==3.7.5

# === Data Validation & CLI ===
pydantic==2.7.4
typer[all]==0.12.3
typer-slim==0.12.3
pyyaml==6.0.1

# === Pronoun Recovery (Phase 2) ===
openai>=1.0.0
seqeval>=1.2.2
scikit-learn>=1.3.0
awesome-align>=0.1.0

# === Utilities ===
tqdm==4.66.4
numpy==1.26.4
nltk==3.8.1

# === Corpus Annotation Storage & Querying ===
pyarrow==24.0.0        # exact-pinned from image inventory (G4)
polars>=0.20.0

# === S3 access (run registry + eval parquets on Nautilus Ceph RGW) ===
boto3==1.42.93         # exact-pinned from image inventory (G4)

# === Annotation Review Web Interface ===
fastapi>=0.100.0
uvicorn[standard]>=0.23.0