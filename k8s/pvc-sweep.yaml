apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pronoun-sweep-data
  namespace: lemn-lab
spec:
  storageClassName: rook-cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
