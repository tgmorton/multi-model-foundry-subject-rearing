# GENERATED by scripts/generate_stage1_early_manifests.py; do not edit.
# phase: architecture-sentinel-gpt2-small
# jobs: 1
# trajectories: 1
# kubectl context/namespace are intentionally not embedded in Kubernetes
# objects. Any future submission must explicitly use:
#   kubectl --context=nautilus ... -n lemn-lab
# No objects have been submitted by this generator.
---
apiVersion: batch/v1
kind: Job
metadata:
  name: thomas-fdy-s1e-sentinel-gpt2s-base-lr30r1
  labels:
    owner: thomas
    study: subject-drop
    stage: train-early-sentinel
    arch: gpt2-small
    lang: en
    intervention: baseline
  annotations:
    foundry.thesis/purpose: stage1-early-seed-lr30-rerun
    foundry.thesis/design: 12-seeds-8-full-parallelism-1
    foundry.thesis/hp-rank: '0'
    foundry.thesis/epochs: '2'
    foundry.thesis/scheduler-epochs: '30'
    foundry.thesis/revision: lr30r1
    foundry.thesis/supersedes: stage1-two-epoch-lr-schedule
    foundry.thesis/code-sha: c5325534fd4e7021d7691a50f771279150c485ca
    foundry.thesis/image-digest: sha256:037b88f45101490ba412890bf431cfefc8c93cba80c7731041649d59aba9a259
    foundry.thesis/seed-set: 42,137,314159,1568568120,1415936399,1640623142,1595274352,2022192329,1891911437,1881877998,302485963,2078344582
    foundry.thesis/slot-map: '[[0,2]]'
spec:
  backoffLimit: 0
  completionMode: Indexed
  completions: 1
  parallelism: 1
  activeDeadlineSeconds: 604800
  template:
    metadata:
      labels:
        owner: thomas
        study: subject-drop
        stage: train-early-sentinel
        arch: gpt2-small
        lang: en
        intervention: baseline
    spec:
      priorityClassName: armada-default
      imagePullSecrets:
      - name: gitlab-registry-cred-thomas
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: nvidia.com/gpu.product
                operator: In
                values:
                - NVIDIA-GeForce-RTX-3090
                - NVIDIA-A10
                - NVIDIA-L4
                - NVIDIA-GeForce-RTX-4090
              - key: kubernetes.io/hostname
                operator: NotIn
                values:
                - gpu-14.nrp.mghpcc.org
                - gpu-17.nrp.mghpcc.org
                - nautilus-it-gpu01.fullerton.edu
                - nautilus-it-gpu03.fullerton.edu
                - rci-tide-gpu-03.sdsu.edu
                - ry-gpu-10.sdsc.optiputer.net
      initContainers:
      - name: clone-repo
        image: alpine/git
        command:
        - /bin/sh
        - -c
        args:
        - |
          set -eu
          cd /opt/repo
          git init .
          git remote add origin https://github.com/tgmorton/multi-model-foundry-subject-rearing.git
          git fetch --depth 1 origin "$GIT_REF"
          git checkout --detach FETCH_HEAD
        env:
        - name: GIT_REF
          value: c5325534fd4e7021d7691a50f771279150c485ca
        resources:
          requests:
            memory: 1Gi
            cpu: 200m
          limits:
            memory: 1Gi
            cpu: 200m
        volumeMounts:
        - name: repo
          mountPath: /opt/repo
      containers:
      - name: trainer
        image: gitlab-registry.nrp-nautilus.io/thmorton/multi-model-foundry-subject-rearing@sha256:037b88f45101490ba412890bf431cfefc8c93cba80c7731041649d59aba9a259
        imagePullPolicy: IfNotPresent
        command:
        - /bin/bash
        - -c
        args:
        - |
          set -euo pipefail
          echo "=========================================="
          echo "  PROD TRAIN — gpt2_small × en × baseline"
          echo "  pod idx=$JOB_COMPLETION_INDEX  (slot resolved by production_agent: SLOT_MAP_JSON if set, else divmod(idx,2))"
          echo "=========================================="
          cat /opt/repo/.git/HEAD 2>/dev/null || true

          # GPU health probe — same 5-sec probe the sweep used.
          python3 -c "
          import sys, torch
          try:
              if not torch.cuda.is_available():
                  print('FATAL: no CUDA', file=sys.stderr); sys.exit(2)
              torch.cuda.synchronize()
              x = torch.zeros(1024, device='cuda'); x.sum().item()
              print(f'  GPU healthy: {torch.cuda.get_device_name(0)}')
          except Exception as e:
              print(f'FATAL: GPU unhealthy: {e}', file=sys.stderr); sys.exit(2)
          "

          cd /opt/repo
          rm -rf /opt/repo/data/raw /opt/repo/data/manipulations /opt/repo/data/tokenized /opt/repo/data/chunked /opt/repo/tokenizers /opt/repo/models
          mkdir -p /mnt/data/tokenized /mnt/data/chunked /mnt/data/tokenizers /mnt/data/models/production
          ln -sfn /mnt/data/raw           /opt/repo/data/raw
          ln -sfn /mnt/data/manipulations /opt/repo/data/manipulations
          ln -sfn /mnt/data/tokenized     /opt/repo/data/tokenized
          ln -sfn /mnt/data/chunked       /opt/repo/data/chunked
          ln -sfn /mnt/data/tokenizers    /opt/repo/tokenizers
          ln -sfn /mnt/data/models        /opt/repo/models
          for d in /opt/repo/data/raw /opt/repo/data/tokenized /opt/repo/data/chunked /opt/repo/tokenizers /opt/repo/models; do
            [ -L "$d" ] || { echo "FAIL $d is not a symlink"; exit 1; }
          done

          # Resolve this Indexed Job's exact predeclared run ID and refuse to write into
          # any existing checkpoint directory.  Completed/partial artifacts require a
          # read-only provenance review and an explicit slot-map revision.
          RUN_ID=$(python3 - <<'PY_SLOT'
          import json, os
          seeds = json.loads(os.environ["SEEDS_JSON"])
          slots = json.loads(os.environ["SLOT_MAP_JSON"])
          _, seed_idx = slots[int(os.environ["JOB_COMPLETION_INDEX"])]
          seed = int(seeds[seed_idx])
          print("gpt2_small-en-baseline-h0-s" + str(seed))
          PY_SLOT
          )
          if [ -e "/mnt/data/models/production/$RUN_ID" ]; then
            RETRY_COUNT="${JOB_INDEX_FAILURE_COUNT:-0}"
            if [ "$RETRY_COUNT" -gt 0 ]; then
              RETRY_ARCHIVE="/mnt/data/models/production/_failed_attempts/${RUN_ID}__index-${JOB_COMPLETION_INDEX}__retry-${RETRY_COUNT}__$(date -u +%Y%m%dT%H%M%SZ)"
              mkdir -p /mnt/data/models/production/_failed_attempts
              test ! -e "$RETRY_ARCHIVE"
              mv "/mnt/data/models/production/$RUN_ID" "$RETRY_ARCHIVE"
              echo "Archived failed index attempt to $RETRY_ARCHIVE"
            else
              echo "FATAL: collision: /mnt/data/models/production/$RUN_ID already exists"
              exit 3
            fi
          fi
          rm -f /tmp/run_succeeded
          python3 scripts/production_agent.py || true
          if [ ! -f /tmp/run_succeeded ]; then
            echo "FAIL run sentinel absent — training did not reach a clean completion"
            exit 1
          fi
          echo "RUN OK: $(cat /tmp/run_succeeded)"
        env:
        - name: PYTHONPATH
          value: /opt/repo
        - name: PYTHONHASHSEED
          value: '0'
        - name: TORCHINDUCTOR_COMPILE_THREADS
          value: '1'
        - name: GIT_REF
          value: c5325534fd4e7021d7691a50f771279150c485ca
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: PYTORCH_CUDA_ALLOC_CONF
          value: expandable_segments:True
        - name: ARCH
          value: gpt2_small
        - name: LANG
          value: en
        - name: INTERVENTION
          value: baseline
        - name: PHYS_BATCH
          value: '16'
        - name: SEEDS_JSON
          value: '[42, 137, 314159, 1568568120, 1415936399, 1640623142, 1595274352,
            2022192329, 1891911437, 1881877998, 302485963, 2078344582]'
        - name: SLOT_MAP_JSON
          value: '[[0, 2]]'
        - name: SAVE_RESUME_LAST_N
          value: '3'
        - name: PROD_EPOCHS
          value: '2'
        - name: SCHEDULER_EPOCHS
          value: '30'
        - name: WANDB_PROJECT_PROD
          value: subject-drop-production
        - name: WANDB_API_KEY
          valueFrom:
            secretKeyRef:
              name: wandb-secret-thomas
              key: WANDB_API_KEY
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: s3-secret-thomas
              key: AWS_ACCESS_KEY_ID
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: s3-secret-thomas
              key: AWS_SECRET_ACCESS_KEY
        - name: AWS_ENDPOINT_URL
          value: http://rook-ceph-rgw-nautiluss3.rook
        - name: AWS_DEFAULT_REGION
          value: us-west-1
        - name: REGISTRY_BUCKET
          value: thomas-subject-drop-artifacts
        - name: DOCKER_IMAGE
          value: gitlab-registry.nrp-nautilus.io/thmorton/multi-model-foundry-subject-rearing@sha256:037b88f45101490ba412890bf431cfefc8c93cba80c7731041649d59aba9a259
        resources:
          requests:
            memory: 4Gi
            cpu: '2'
            nvidia.com/gpu: 1
          limits:
            memory: 4Gi
            cpu: '2'
            nvidia.com/gpu: 1
        volumeMounts:
        - name: repo
          mountPath: /opt/repo
        - name: data
          mountPath: /mnt/data
      volumes:
      - name: repo
        emptyDir: {}
      - name: data
        persistentVolumeClaim:
          claimName: subject-drop-archive
      restartPolicy: Never
      tolerations:
      - key: nvidia.com/gpu
        operator: Exists
        effect: NoSchedule
      automountServiceAccountToken: false
      enableServiceLinks: false
