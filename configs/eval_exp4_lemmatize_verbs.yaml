# ===================================================================
# EVALUATION CONFIGURATION - Experiment 4: Lemmatize Verbs
# Configuration for evaluating the model trained with lemmatized verbs
# ===================================================================

evaluation:
  # --- Model Configuration ---
  model_checkpoint_dir: "models/exp4_lemmatize_verbs/"
  tokenizer_path: "tokenizers/exp4_lemmatize_verbs/"
  
  # --- Test Datasets ---
  test_corpus: "data/raw/test_10M/"
  blimp_dir: "evaluation/stimuli/blimp/"
  null_subject_dir: "evaluation/stimuli/null-subj/"
  
  # --- Evaluation Settings ---
  batch_size: 64
  device: "auto"
  use_fp16: true
  max_length: 1000
  
  # --- Analysis Options ---
  run_perplexity: true
  run_blimp: true  # Important for verb agreement and morphology tests
  run_null_subject: true
  
  # --- Speed Optimizations ---
  num_workers: 4
  pin_memory: true
  prefetch_factor: 2
  
  # --- Output Configuration ---
  output_dir: "evaluation/results/exp4_lemmatize_verbs/"
  save_detailed: true
  save_format: "jsonl"